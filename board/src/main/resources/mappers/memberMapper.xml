<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.board.mappers.memberMapper">

	<insert id="register">
		insert into myMember(userId, userPwd, userName, genDer)
			values(#{userId}, #{userPwd}, #{userName}, #{genDer})
	</insert>
	<!--  table에 값을 추가하는 insert query -->
	
	<select id="login" resultType="com.board.domain.MemberVO">
		select userId,userName
		from myMember
		where userId = #{userId} and userPwd = #{userPwd};
	</select>
</mapper>